# Companion

This app will run on the developer's machine and bridge the web UI to local Roblox development tools.

Initial responsibilities:

- discover local project state
- scan `Rojo` and `Wally` files
- talk to Roblox Studio MCP
- apply approved patches
- expose a safe local API to the web app
