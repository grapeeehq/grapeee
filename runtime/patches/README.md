# Runtime Patch Notes

Keep Grapeee runtime changes narrow and documented here so the Rojo fork can stay close to upstream.

Initial patch themes:

1. Add agent-facing runtime API.
2. Add snapshot metadata before mutation.
3. Expose Studio tree and selection in a compact JSON shape.
4. Stream logs and playtest status where possible.
5. Preserve upstream protocol compatibility until a change is clearly necessary.

