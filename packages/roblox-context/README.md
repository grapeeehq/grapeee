# Roblox Context

This package will contain Roblox-aware project analysis logic.

Initial scope:

- parse `Rojo` project files
- inspect `Wally` manifests
- classify server, client, shared, and UI code
- produce compact context bundles for the agent
